# セキュリティ運用

## 秘密情報

- OpenAI APIキー、端末接続コード、端末鍵はこのリポジトリに保存しません。
- `public-config.js` のGAS URLは公開アプリの固定接続先であり、認証情報ではありません。
- `.env*`、実行ログ、バックアップ、画面検証ファイルは `.gitignore` でGitの対象外にします。

## 端末の接続と失効

- 新しい端末鍵はGASとMacの両方へ登録し、Macから指紋を読み戻した後だけ使用を開始します。
- 再接続時は新しい鍵へ切り替えた後、以前の端末IDをMac側で失効させます。
- 設定画面の「この端末の接続を解除」は、Mac側で失効成功を確認するまで端末内の鍵を消しません。
- 登録・一覧確認・失効は、秘密値を含まないMac側の永続監査記録で追跡します。

## 端末内の写真

- 現在の入力と一時保存の写真はブラウザ内だけに保存します。
- 最終更新から30日を過ぎた写真付きデータは、次回起動時に自動削除します。
- 早く消す場合は一時保存の「削除」または入力画面のリセットを使います。

## バックアップと復旧確認

```sh
scripts/create_backup.sh
scripts/verify_backup.sh backups/YYYYMMDD-HHMMSS
```

`verify_backup.sh` は作業中のファイルを変更せず、一時フォルダへ復元して構文・セキュリティ・回帰テストを実行します。

## 更新前の確認

1. `scripts/run_checks.sh` を通す。
2. `sw.js` の `CACHE_NAME`、HTMLのキャッシュ用番号、画面上のバージョンを同時に更新する。
3. スマホ幅のブラウザで、初期設定・端末確認・通常入力の表示を確認する。
4. GitHub Pagesの公開URLから最新バージョンとService Workerを読み戻す。
